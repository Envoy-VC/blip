import React from 'react';
import { Profile } from '@lens-protocol/react-web';

interface Props {
	profile: Profile;
}

const ChannelStatistics = ({ profile }: Props) => {
	return <div className='border-2'>ChannelStatistics</div>;
};

export default ChannelStatistics;
